html
  head
    title Angular 2 QuickStart
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1")
    link(rel="stylesheet", href="stylesheets/hero-dependencies.css")
    link(rel="stylesheet", href="stylesheets/style.css")

    script(src="deps.js")
    script(src="javascripts/systemjs.config.js")
    script
      | System.config({
      |   defaultJSExtensions: true,
      |   packages: {
      |     app: {
      |       format: 'register',
      |       defaultExtension: 'js'
      |     }
      |   }
      | });
      | System.import('ng2/main')
      |       .then(null, console.error.bind(console));
    link(href="https://fonts.googleapis.com/icon?family=Material+Icons", rel="stylesheet")
  body
    hero
      .spin-area.layout-column.layout-align-center-center
        .fa.fa-spinner.fa-spin.fa-5x
